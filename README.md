# Real-Time-Price-Tracker
This repo is code base for an iOS application using SwiftUI that displays real-time price updates for multiple stock symbols (e.g., NVDA, AAPL, GOOG, etc.) and supports a second screen for symbol details.
